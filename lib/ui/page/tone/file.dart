import 'package:flutter/material.dart';

class ToneFilePage extends StatelessWidget {
  const ToneFilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}